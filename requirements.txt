# Python dependencies for the Exam Proctoring module (exam_proctor.py)
# Install with: pip install -r requirements.txt
#
# NOTE: face_recognition requires dlib, which needs CMake and a C++ compiler.
# On Windows, install CMake first:  pip install cmake
# Alternatively, use conda:        conda install -c conda-forge dlib face_recognition

mediapipe>=0.10.0
opencv-python>=4.8.0
face_recognition>=1.3.0
numpy>=1.24.0
